import React, { Component } from "react";
import { Typography } from "@material-ui/core";

export default class NotFindPage extends Component {
  render() {
    return (
      <div>
        <Typography variant={"h6"}>Sorry, the page does not exist!</Typography>
      </div>
    );
  }
}
